# Dataset

Place `letters.csv` in this folder before running the notebook.

The dataset used in this project contains 42,000 handwritten digit samples, a `label` column, and 45 pixel-intensity features.

The original dataset is not included in this repository.
